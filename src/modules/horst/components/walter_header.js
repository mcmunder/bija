import React from 'react'
import styled from 'styled-components'

const WalterHeaderBase = styled.div`

`

const WalterHeader = () => (
  <WalterHeaderBase>
    WalterHeader
  </WalterHeaderBase>
)

export default WalterHeader
